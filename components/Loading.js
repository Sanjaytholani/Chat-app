import { Circle } from "better-react-spinkit";
const Loading = () => {
  return (
    <div className="flex flex-col justify-center items-center h-screen">
      <Circle color="#111827" size={100} />
    </div>
  );
};

export default Loading;
